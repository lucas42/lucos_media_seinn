services:
  seinn:
    build: .
    container_name: lucos_media_seinn
    ports: 
      - $PORT:$PORT
    environment:
      - MEDIA_MANAGER_URL=https://ceol.l42.eu/
      - KEY_LUCOS_MEDIA_MANAGER
      - KEY_LUCOS_PRIVATE
      - ENVIRONMENT
      - PORT
      - APP_ORIGIN
      - AITHNE_ORIGIN
    image: lucas42/lucos_media_seinn:${VERSION:-latest}
    restart: always
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:${PORT}/_info"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s